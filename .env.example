# Path where model weights are stored on the network volume.
# On RunPod this is typically /runpod-volume/models
MODEL_PATH=/runpod-volume/models

# Hugging Face cache directory (avoids re-downloading tokenizers etc.)
HF_HOME=/runpod-volume/.cache/huggingface

# Optional: Hugging Face token (required for gated models)
# HUGGING_FACE_HUB_TOKEN=hf_...

# Optional: RUNPOD_DEBUG_LEVEL (default: INFO)
# RUNPOD_DEBUG_LEVEL=DEBUG
